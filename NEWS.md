# fixedincome 0.0.1.9999

* Added `progagate` slot to `Interpolation` class. It defines if the interpolation method is propagated to interpolated curves.
* Code refactoring

# fixedincome 0.0.1

* First CRAN version.